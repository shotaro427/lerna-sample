{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.0.2"
  },
  "scripts": {
    "up-version:client": "lerna version --no-private --allow-branch master --tag-version-prefix client/v --conventional-commits"
  }
}
